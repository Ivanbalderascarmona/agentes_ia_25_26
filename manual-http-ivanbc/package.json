{
  "name": "manual-http-ivanbc",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server:up": "json-server --watch src/db/db.json --port 4000",
    "crud:curl": "node src/crud-curl.js",
    "validate": "bash scripts/validate.sh"
  },
  "author": "Iván Balderas Carmona",
  "license": "ISC",
  "description": "Sistema de gestión mediante peticiones HTTP usando una API local con json-server",
  "dependencies": {
    "dotenv": "^17.2.3",
    "json-server": "^1.0.0-beta.3"
  }
}
